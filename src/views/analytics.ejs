<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/styles/output.css">
    <title>URL Analytics</title>
</head>
<body class="bg-slate-900 w-full h-screen p-6 flex flex-col items-center space-y-10">
    <h1 class="text-5xl font-bold text-white">URL Shortener</h1>

    <div class="w-fit ring-2 rounded-md flex flex-col px-10 py-4 bg-white space-y-6 items-center shadow-sm shadow-white">
        <h1 class="text-gray-500 font-bold text-2xl text-center">Total URL Clicks</h1>

        <p class="text-center">The number of clicks from the shortened URL that redirected the user to the destination page.</p>
        
        <div class="relative overflow-x-auto">
            <table class="w-full text-sm text-left rtl:text-right rounded-md overflow-hidden text-gray-200 dark:text-gray-400">
                <thead class="text-sm text-gray-200 uppercase  bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr class="divide-x divide-solid divide-white">
                        <th scope="col" class="px-6 py-3">
                            Shortened URL
                        </th>
                        <th scope="col" class="px-6 py-3">
                            Full URL
                        </th>
                        <th scope="col" class="px-6 py-3">
                            Total Clicks
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="divide-x divide-solid divide-white bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                        <td class="px-6 py-4">
                            <%= shortURL %>
                        </td>
                        <td class="px-6 py-4">
                            <%= originalURL %>
                        </td>
                        <td class="px-6 py-4">
                            <%= visitCount %>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <button class="bg-blue-600 px-4 py-2 ml-1 rounded-sm border-none outline-none hover:bg-blue-700 active:scale-95 text-white font-medium tracking-wide" id="regenerateBtn">Shorten Another URL</button>

    <script src="/javascript/index.js"></script>
</body>
</html>