<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/styles/output.css">
    <title>Shortened URL</title>
</head>
<body class="bg-slate-900 w-full h-screen p-6 flex flex-col items-center space-y-10">
    <h1 class="text-5xl font-bold text-white">URL Shortener</h1>

    <div class="w-1/2 ring-2 rounded-md flex flex-col px-10 py-4 bg-white space-y-6 items-center shadow-sm shadow-white">
        <h1 class="text-gray-500 font-bold text-2xl">Your shortened URL</h1>

        <form class="flex w-full">
            <input type="text" value=<%= shortURL %> readonly class="py-3 px-2 border-none flex-grow bg-slate-200" id="shortUrlDisplay">
            <button class="bg-blue-600 px-4 py-2 ml-1 rounded-sm border-none outline-none hover:bg-blue-700 active:scale-95 text-white font-medium tracking-wide" id="copyBtn">Copy</button>
        </form>

        <div class="flex justify-start gap-2 w-full">
            <p class="font-medium">Long URL: </p>
            <a class="text-blue-500" href=<%= fullURL %>><%= fullURL %></a>
        </div>

        <div class="space-x-4">
            <button class="bg-blue-600 px-4 py-2 ml-1 rounded-sm border-none outline-none hover:bg-blue-700 active:scale-95 text-white font-medium tracking-wide" id="showAnalyticsBtn" data-shortId=<%= shortID %>>Show Analytics</button>

            <button class="bg-blue-600 px-4 py-2 ml-1 rounded-sm border-none outline-none  hover:bg-blue-700 active:scale-95 text-white font-medium tracking-wide" id="regenerateBtn">Shorten Another URL</button>
        </div>

        <p>Copy the short link and share it in messages, texts, posts, websites and other locations.</p>
    </div>

    <script src="/javascript/index.js"></script>
</body>
</html>